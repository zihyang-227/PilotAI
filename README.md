# healthAI_demo

1. create <.env> file that consists of: OPENAI_API_KEY=(put the API KEY HERE)

2. files ends with <.vtt> = transcripts

3. <prompt.csv> = prompt
4. <output.csv> = output structure
5. <code(file1 trial).py> = python code
6. <G30_1_Analysis.csv> = result 
